[project]
name = "orchestrator-workers"
version = "0.1.0"
description = "Project 4: Orchestrator Workers Pattern"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.30.0",
    "python-dotenv",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

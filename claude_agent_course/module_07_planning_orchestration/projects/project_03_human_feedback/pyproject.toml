[project]
name = "human-feedback"
version = "0.1.0"
description = "Project 3: Human-in-the-loop"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.30.0",
    "python-dotenv",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

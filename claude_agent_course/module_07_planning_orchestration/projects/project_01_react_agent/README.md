# Project 1: ReAct Agent Engine

> "不要仅仅使用 Agent，要成为 Agent 的创造者。"

## 🎯 目标
本项目的目标不是调用现成的 `Agent().run()`，而是**揭开 Agent 的黑盒**。我们将手动编写代码来实现 ReAct (Reasoning + Acting) 循环。

你将亲手实现：
1.  **Parsing**: 正则表达式解析 LLM 输出的 `Action: xxx` 和 `Action Input: xxx`。
2.  **Execution**: 动态执行工具函数。
3.  **Basic Loop**: `While` 循环，将 Observation 拼接回 Prompt。

## 🛠️ 任务清单
1.  在 `src/tools.py` 中实现简单的工具（如计算器）。
2.  在 `src/prompts.py` 中完善 ReAct 系统提示词。
3.  在 `src/agent.py` 中编写 `step()` 函数和 `run()` 循环。
4.  在 `src/main.py` 中测试你的 Agent。

## 🚀 运行
```bash
# 安装依赖
pip install -e .

# 运行（你需要先完成代码）
python -m src.main
```

## 🧠 思考题
- 如果 Agent 输出的格式不对（例如忘了写 `Action Input`），你的代码会崩吗？如何让它更健壮？ -> *Self-Correction*

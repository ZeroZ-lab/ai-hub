[project]
name = "travel-planner"
version = "0.1.0"
description = "Project 2: Plan-and-Solve 模式"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.30.0",
    "python-dotenv",
    "pydantic" 
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[project]
name = "weather-assistant"
version = "0.1.0"
description = "天气查询助手 - 自定义工具实战"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

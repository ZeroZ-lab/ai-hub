# Hello Agent - 你的第一个 AI Agent

> **难度**: 🌟 初级
> **预计时间**: 30-45 分钟

---

## 项目背景

这是你的第一个 Claude Agent 项目！通过这个项目，你将学会：
1. 搭建 Agent 开发环境
2. 调用 Claude API
3. 实现基础的对话功能

**示例场景**：
> 你想创建一个 AI 助手，让它能够读取当前目录的文件列表，并用自然语言告诉你有哪些文件。

---

## 学习目标

完成本项目后，你将能够：
- [ ] 正确配置 Anthropic API Key
- [ ] 使用 Python 调用 Claude API
- [ ] 理解 Agent 的基本工作流程
- [ ] 实现简单的文件系统查看功能

---

## 功能需求

### 必做功能 (Core)

#### 1. 环境检测
- 输入：无
- 输出：显示 Python 版本、当前目录路径
- 示例：
  ```
  Python 版本: 3.10.12
  当前目录: /Users/xxx/my_project
  ```

#### 2. 列出文件
- 输入：目录路径（可选，默认当前目录）
- 输出：用自然语言描述文件列表
- 示例：
  ```
  你: 告诉我当前目录有什么文件
  Agent: 当前目录包含 5 个文件：
  1. README.md - 项目说明文档
  2. main.py - Python 脚本
  3. .env - 环境变量配置
  4. data/ - 数据文件夹
  5. tests/ - 测试目录
  ```

#### 3. 文件统计
- 输入：目录路径
- 输出：文件类型统计
- 示例：
  ```
  统计结果：
  - Python 文件 (.py): 3 个
  - Markdown 文件 (.md): 2 个
  - 目录: 2 个
  ```

### 选做功能 (Advanced)

- [ ] **进阶 1**：支持递归查看子目录
- [ ] **进阶 2**：根据文件名猜测文件用途
- [ ] **进阶 3**：生成目录结构树状图

---

## 技术要点

### 核心概念
- **API 调用**：使用 `anthropic` 库与 Claude 交互
- **系统信息**：使用 Python 的 `os` 模块读取文件系统
- **提示工程**：如何让 Agent 理解你的需求

### 使用的 API
```python
# 核心 API
client.messages.create(
    model="claude-3-5-sonnet-20241022",
    max_tokens=1024,
    messages=[...]
)

# 文件操作
os.listdir(path)
os.path.isfile(path)
os.path.isdir(path)
```

### 注意事项
⚠️ **重点**：
1. 确保 `.env` 文件不被提交到 Git
2. 第一次运行前必须配置 API Key
3. 注意 API 调用的错误处理

---

## 实现步骤

### 准备工作

#### 1. 环境配置
```bash
# 进入项目目录
cd claude_agent_course/module_01_foundations/projects/project_01_hello_agent

# 使用 uv 初始化
uv init --no-readme

# 安装依赖
uv add anthropic python-dotenv

# 配置 API Key
cp .env.example .env
# 编辑 .env，填入你的 ANTHROPIC_API_KEY
```

#### 2. 项目结构
```
project_01_hello_agent/
├── README.md              # 本文件
├── pyproject.toml         # uv 项目配置
├── uv.lock                # 依赖锁定
├── .env.example           # 环境变量示例
├── .env                   # 你的环境变量（不要提交）
├── src/
│   ├── __init__.py
│   ├── main.py           # 主程序（待实现）
│   └── agent.py          # Agent 类（待实现）
├── tests/
│   ├── __init__.py
│   └── test_agent.py     # 测试（可选）
└── solution/
    └── main.py           # 参考答案
```

---

### 第一步：实现环境检测

**目标**：创建一个函数，显示 Python 版本和当前目录

**任务**：
1. 在 `src/main.py` 中创建 `show_environment()` 函数
2. 使用 `sys.version` 获取 Python 版本
3. 使用 `os.getcwd()` 获取当前目录

**提示**：
```python
import sys
import os

def show_environment():
    """显示环境信息"""
    print(f"Python 版本: {sys.version}")
    print(f"当前目录: {os.getcwd()}")

# TODO: 实现这个函数
```

**验证**：
```bash
uv run python src/main.py
# 预期输出: Python 版本和当前目录信息
```

---

### 第二步：实现文件列表功能

**目标**：读取目录中的文件和文件夹

**任务**：
1. 在 `src/agent.py` 中创建 `get_file_list()` 函数
2. 使用 `os.listdir()` 获取文件列表
3. 区分文件和目录（使用 `os.path.isfile()` 和 `os.path.isdir()`）

**提示**：
```python
import os

def get_file_list(directory="."):
    """获取目录中的文件列表

    Args:
        directory: 目录路径，默认当前目录

    Returns:
        dict: {"files": [...], "directories": [...]}
    """
    # TODO: 实现这里
    pass
```

**验证**：
```bash
# 测试获取文件列表
uv run python -c "from src.agent import get_file_list; print(get_file_list())"
```

---

### 第三步：连接 Claude API

**目标**：让 Agent 能够理解文件列表并用自然语言描述

**任务**：
1. 创建 Anthropic 客户端
2. 将文件列表发送给 Claude
3. 获取自然语言描述

**提示**：
```python
import os
from anthropic import Anthropic
from dotenv import load_dotenv

load_dotenv()

class FileAgent:
    def __init__(self):
        api_key = os.getenv("ANTHROPIC_API_KEY")
        if not api_key:
            raise ValueError("请设置 ANTHROPIC_API_KEY")
        self.client = Anthropic(api_key=api_key)

    def describe_files(self, file_list):
        """让 Claude 描述文件列表"""
        # TODO: 实现 API 调用
        prompt = f"请用自然语言描述以下文件列表：\n{file_list}"

        response = self.client.messages.create(
            model="claude-3-5-sonnet-20241022",
            max_tokens=512,
            messages=[{"role": "user", "content": prompt}]
        )

        return response.content[0].text
```

**验证**：
```bash
uv run python src/main.py
# 预期输出: Claude 对文件列表的自然语言描述
```

---

### 第四步：实现交互式对话

**目标**：让用户可以通过对话询问文件信息

**任务**：
1. 实现一个交互循环
2. 接收用户输入
3. 根据输入执行相应操作

**提示**：
```python
def main():
    """主函数"""
    print("=" * 60)
    print("Hello Agent - 文件查看助手")
    print("=" * 60)
    print("\n命令:")
    print("  ls    - 列出当前目录文件")
    print("  stat  - 统计文件类型")
    print("  exit  - 退出")
    print("=" * 60)

    agent = FileAgent()

    while True:
        user_input = input("\n你: ").strip()

        if user_input.lower() == 'exit':
            print("再见！")
            break

        if user_input.lower() == 'ls':
            # TODO: 调用 agent.describe_files()
            pass

        if user_input.lower() == 'stat':
            # TODO: 实现文件统计
            pass
```

---

## 测试方法

### 功能测试

#### 测试用例 1：环境检测
```bash
uv run python src/main.py

# 预期输出:
# Python 版本: 3.10.x
# 当前目录: /path/to/project
```

#### 测试用例 2：文件列表
```bash
# 在交互模式中输入
你: ls

# 预期输出:
# Agent: 当前目录包含以下文件：
# 1. README.md - 项目说明
# 2. src/ - 源代码目录
# ...
```

#### 测试用例 3：文件统计
```bash
你: stat

# 预期输出:
# Agent: 文件统计结果：
# - Python 文件: 3 个
# - Markdown 文件: 1 个
# - 目录: 2 个
```

---

## 完成标准

### ✅ 基础版（60 分）
- [ ] 能运行并显示环境信息
- [ ] 能列出当前目录的文件
- [ ] Claude 能用自然语言描述文件

### ✅ 良好版（80 分）
- [ ] 基础版所有要求
- [ ] 实现了文件统计功能
- [ ] 添加了错误处理
- [ ] 代码有适当注释

### ✅ 优秀版（100 分）
- [ ] 良好版所有要求
- [ ] 完成了至少一个进阶功能
- [ ] 代码结构清晰，易于扩展
- [ ] 有良好的用户体验（提示清晰）

---

## 扩展挑战

### 挑战 1：智能文件分类
**难度**: 🌟🌟
**描述**: 让 Agent 根据文件名和内容猜测文件用途
**提示**: 可以让 Claude 分析文件扩展名，推测项目类型

### 挑战 2：生成项目报告
**难度**: 🌟🌟🌟
**描述**: 让 Agent 生成一个项目结构报告（Markdown 格式）
**提示**: 可以使用树状图展示目录结构

### 挑战 3：文件搜索
**难度**: 🌟🌟🌟
**描述**: 实现模糊搜索功能，找到包含特定关键词的文件
**提示**: 使用 `os.walk()` 递归遍历目录

---

## 常见问题

### Q1: API Key 错误怎么办？
**A**:
1. 检查 `.env` 文件是否存在
2. 确认 `ANTHROPIC_API_KEY=sk-ant-...` 格式正确
3. 重新生成 API Key

### Q2: 如何调试 API 调用？
**A**:
```python
# 打印完整响应
print(response.model_dump())

# 查看 Token 使用
print(f"输入: {response.usage.input_tokens}")
print(f"输出: {response.usage.output_tokens}")
```

### Q3: 遇到困难怎么办？
**A**:
1. 查看 [docs/03_最佳实践.md](../docs/03_最佳实践.md)
2. 参考 `solution/main.py` 中的实现
3. 重新阅读 [docs/02_代码示例.md](../docs/02_代码示例.md)

---

## 参考资源

- [Module 1 概念讲解](../docs/01_概念讲解.md)
- [Module 1 代码示例](../docs/02_代码示例.md)
- [Module 1 最佳实践](../docs/03_最佳实践.md)
- [Anthropic API 文档](https://docs.anthropic.com/claude/reference)

---

**开始你的 Agent 之旅吧！🚀**
